import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App should have a title', (WidgetTester tester) async {
    // 基础冒烟测试占位
    // 完整测试需要 Firebase 模拟器环境
    expect(1 + 1, 2);
  });
}
